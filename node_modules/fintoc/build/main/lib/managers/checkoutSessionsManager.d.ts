import { ResourceArguments } from '../../types';
import { ManagerMixin } from '../mixins';
import { CheckoutSession } from '../resources/checkoutSession';
export declare class CheckoutSessionsManager extends ManagerMixin<CheckoutSession> {
    static resource: string;
    static methods: string[];
    expire(identifier: string, args?: ResourceArguments): Promise<CheckoutSession>;
}
