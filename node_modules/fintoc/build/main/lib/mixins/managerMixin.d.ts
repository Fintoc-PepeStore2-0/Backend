import { IResourceMixin } from '../../interfaces/mixins';
import { ResourceArguments } from '../../types';
import { Client } from '../client';
export declare abstract class ManagerMixin<ResourceType extends IResourceMixin> {
    #private;
    static resource: string;
    static methods: string[];
    protected _client: Client;
    constructor(path: string, client: Client);
    /**
     * Return all the instances of the resource being handled by the manager.
     *
     * @param args - Object with the arguments to filter the query, using the API parameters
     * @returns All the instances of the resource
     */
    list(args?: ResourceArguments & {
        lazy: true;
    }): Promise<AsyncGenerator<ResourceType>>;
    list(args?: ResourceArguments & {
        lazy: false;
    }): Promise<ResourceType[]>;
    list(args?: ResourceArguments): Promise<AsyncGenerator<ResourceType>>;
    /**
     * Return all the instances of the resource being handled by the manager.
     *
     * @deprecated Use list() instead
     * @param args - Object with the arguments to filter the query, using the API parameters
     * @returns All the instances of the resource
     */
    all(args?: ResourceArguments & {
        lazy: true;
    }): Promise<AsyncGenerator<ResourceType>>;
    all(args?: ResourceArguments & {
        lazy: false;
    }): Promise<ResourceType[]>;
    all(args?: ResourceArguments): Promise<AsyncGenerator<ResourceType>>;
    /**
     * Return an instance of the resource being handled by the manager.
     *
     * @param identifier - The identifier of the resource to get
     * @param args - Object with the arguments to filter the query, using the API parameters
     * @returns An instance of the resource
     */
    get(identifier: string, args?: ResourceArguments): Promise<ResourceType>;
    /**
     * Create an instance of the resource being handled by the manager.
     *
     * @param args - Data to be passed for the object creation, as specified by the API
     * @returns The created instance of the object
     */
    create(args?: ResourceArguments): Promise<ResourceType>;
    /**
     * Update an instance of the resource being handled by the manager.
     *
     * @param identifier - The identifier of the resource to update
     * @param args - Data to be passed for the object to be updated with, as specified by the API
     * @returns The updated instance of the object
     */
    update(identifier: string, args?: ResourceArguments): Promise<ResourceType>;
    /**
     * Delete an instance of the resource being handled by the manager.
     *
     * @param identifier - The identifier of the resource to delete
     * @param args - Object with the arguments to filter the query, using the API parameters
     * @returns The identifier of the deleted object
     */
    delete(identifier: string, args?: ResourceArguments): Promise<string>;
    protected _list(args?: ResourceArguments): Promise<ResourceType[] | AsyncGenerator<ResourceType>>;
    protected _get(identifier: string, args?: ResourceArguments): Promise<ResourceType>;
    protected _create(args?: ResourceArguments): Promise<ResourceType>;
    protected _update(identifier: string, args?: ResourceArguments): Promise<ResourceType>;
    protected _delete(identifier: string, args?: ResourceArguments): Promise<string>;
    protected postListHandler(objects: ResourceType[] | AsyncGenerator<ResourceType>, args: ResourceArguments): AsyncGenerator<ResourceType, any, any> | ResourceType[];
    protected postGetHandler(object: ResourceType, identifier: string, args: ResourceArguments): ResourceType;
    protected postCreateHandler(object: ResourceType, args: ResourceArguments): ResourceType;
    protected postUpdateHandler(object: ResourceType, identifier: string, args: ResourceArguments): ResourceType;
    protected postDeleteHandler(identifier: string, args: ResourceArguments): string;
    protected buildPath(args?: ResourceArguments): string;
}
