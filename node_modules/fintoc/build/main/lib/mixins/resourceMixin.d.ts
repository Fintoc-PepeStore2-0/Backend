import { GenericFunction, ResourceArguments } from '../../types';
import { Client } from '../client';
export declare abstract class ResourceMixin<ResourceType> {
    #private;
    static mappings: {};
    static resourceIdentifier: string;
    protected _client: Client;
    [anyAttribute: string]: any;
    constructor(client: Client, handlers: Record<string, GenericFunction>, methods: string[], path: string, content: Record<string, any>);
    protected static _build(client: Client, handlers: Record<string, GenericFunction>, methods: string[], path: string, data: Record<string, any>): Promise<any>;
    protected _useClient(): Client;
    protected _updateClient(newClient: Client): void;
    serialize(): {};
    /**
     * Update this instance of the resource.
     *
     * @param args - Data to be passed for the object to be updated with, as specified by the API
     * @returns The updated instance of the object
     */
    update(args?: ResourceArguments): Promise<ResourceType>;
    /**
     * Delete this instance of the resource.
     *
     * @param args - Object with the arguments to filter the query, using the API parameters
     * @returns The identifier of the deleted object
     */
    delete(args?: ResourceArguments): Promise<string>;
    private _update;
    private _delete;
}
