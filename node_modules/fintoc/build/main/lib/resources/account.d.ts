import { GenericFunction } from '../../types';
import { Client } from '../client';
import { MovementsManager } from '../managers';
import { ResourceMixin } from '../mixins/resourceMixin';
export declare class Account extends ResourceMixin<Account> {
    #private;
    constructor(client: Client, handlers: Record<string, GenericFunction>, methods: string[], path: string, content: Record<string, any>);
    /**
     * @deprecated This method is deprecated and will be removed in a future version.
     * Please use the Fintoc client directly to access movements. See: https://github.com/fintoc-com/fintoc-node/pull/34
     */
    get movements(): MovementsManager;
    set movements(newValue: MovementsManager);
}
