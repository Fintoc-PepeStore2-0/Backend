import { IPaginationOptions } from '../interfaces/paginator';
/**
 * Parse a link string and return an object with previous parsed links and the new one.
 *
 * @param object - Object containing already parsed links
 * @param link - String with the link header to parse
 * @returns Object that corresponds to the destructured `object` object and the parsed `link` string
 */
export declare function parseLink(object: Record<string, string>, link: string): Record<string, string>;
/**
 * Parse the 'link' header into an object.
 *
 * @param linkHeader - String containing the 'link' header of an HTTP request
 * @returns An object with the link headers parsed link headers
 */
export declare function parseLinkHeaders(linkHeader: null): null;
export declare function parseLinkHeaders(linkHeader: undefined): undefined;
export declare function parseLinkHeaders(linkHeader: string): Record<string, string>;
export declare function parseLinkHeaders(linkHeader: any): Record<string, string> | null | undefined;
export declare function request(options: IPaginationOptions): Promise<{
    next: string | null | undefined;
    elements: Record<string, any>[];
}>;
export declare function paginate(options: IPaginationOptions): AsyncGenerator<Record<string, any>, void, unknown>;
