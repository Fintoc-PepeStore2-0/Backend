import { ResourceMixin } from '../mixins/resourceMixin';
export declare class Invoice extends ResourceMixin<Invoice> {
    static mappings: {
        issuer: string;
        receiver: string;
    };
}
